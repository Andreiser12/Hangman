<Window x:Class="HangmanGame.Views.NewUserWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="clr-namespace:HangmanGame.ViewModels"
        xmlns:conv="clr-namespace:HangmanGame.Converters"
        Title="New User" Height="450" Width="350"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterScreen">

    <Window.DataContext>
        <vm:NewUserViewModel/>
    </Window.DataContext>

    <Window.Background>
        <ImageBrush ImageSource="pack://application:,,,/Resources/Backgrounds/all_categories.jpg" 
                Stretch="UniformToFill"/>
    </Window.Background>

    <Window.Resources>
        <conv:RelativePathToImageConverter x:Key="ImageConverter"/>
        <conv:BoolToVisibilityConverter x:Key="BoolToVis"/>
    </Window.Resources>

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0">
            <TextBlock Text="Username:" Margin="0,0,0,5"/>
            <TextBox Text="{Binding Username, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" 
                     Margin="0,0,0,15"
                     Background="#AAE8F0F5"
                     BorderBrush="#607080"
                     Foreground="Black"
                     FontSize="14"/>
        </StackPanel>

        <StackPanel Grid.Row="1" Visibility="{Binding ShowImageSection, Converter={StaticResource BoolToVis}}">
            <TextBlock Text="Image:" Margin="0,0,0,5"/>
            <Button Content="Browse an image..." 
                    Command="{Binding BrowseImageCommand}" 
                    Width="150" 
                    HorizontalAlignment="Center"
                    Margin="0,0,0,15"/>
        </StackPanel>

        <StackPanel Grid.Row="2" Visibility="{Binding ShowImageSection, Converter={StaticResource BoolToVis}}">
            <TextBlock Text="OR" 
                   HorizontalAlignment="Center" 
                   FontWeight="Bold" 
                   Margin="0,10,0,5"/>
            <TextBlock Text="Choose an avatar" 
                   HorizontalAlignment="Center" 
                   Margin="0,0,0,10"/>

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <Button Grid.Column="0" 
                    Content="&lt;&lt;" 
                    Command="{Binding PreviousAvatarCommand}"
                    Width="40" Height="40"
                    VerticalAlignment="Center"/>

                <Image Grid.Column="1" 
                   Source="{Binding ImagePath, Converter={StaticResource ImageConverter}}" 
                   Height="180" 
                   Stretch="Uniform"/>

                <Button Grid.Column="2" 
                    Content="&gt;&gt;" 
                    Command="{Binding NextAvatarCommand}"
                    Width="40" Height="40"
                    VerticalAlignment="Center"/>
            </Grid>
        </StackPanel>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,15,0,0">
            <Button Content="OK" Width="75" Margin="0,0,10,0" 
                Click="OkButton_Click" IsDefault="True"/>
            <Button Content="Cancel" Width="75" 
                Click="CancelButton_Click" IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>