<Window x:Class="HangmanGame.Views.OpenGameWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Open Game" Height="400" Width="500"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize">

    <Window.Background>
        <ImageBrush ImageSource="pack://application:,,,/Resources/Backgrounds/all_categories.jpg" 
                Stretch="UniformToFill"/>
    </Window.Background>

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" 
                   Text="Select a saved game to continue:" 
                   FontSize="14" 
                   Margin="0,0,0,10"/>

        <ListBox Grid.Row="1"
                 ItemsSource="{Binding SavedGames}"
                 SelectedItem="{Binding SelectedGame, Mode=TwoWay}"
                 FontSize="13"
                 Background="#AAE8F0F5">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal" Margin="5">
                        <TextBlock Text="{Binding GameName}" 
                                   FontWeight="Bold" 
                                   Width="150"/>
                        <TextBlock Text="{Binding Category, StringFormat='Category: {0}'}" 
                                   Margin="10,0,0,0"/>
                        <TextBlock Text="{Binding CurrentLevel, StringFormat='Level: {0}/3'}" 
                                   Margin="10,0,0,0"/>
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <Button Grid.Row="2" 
                Content="Delete Selected Game" 
                Command="{Binding DeleteGameCommand}"
                HorizontalAlignment="Left"
                Width="180"
                Margin="0,10,0,0"/>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,15,0,0">
            <Button Content="Open" Width="75" Margin="0,0,10,0" 
                    Click="OpenButton_Click" IsDefault="True"/>
            <Button Content="Cancel" Width="75" 
                    Click="CancelButton_Click" IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>