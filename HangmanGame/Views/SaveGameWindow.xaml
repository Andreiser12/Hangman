<Window x:Class="HangmanGame.Views.SaveGameWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Save Game" Height="180" Width="350"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize">

    <Window.Background>
        <ImageBrush ImageSource="pack://application:,,,/Resources/Backgrounds/all_categories.jpg" 
                Stretch="UniformToFill"/>
    </Window.Background>

    <StackPanel Margin="15" VerticalAlignment="Center">
        <TextBlock Text="Enter a name for your saved game:" Margin="0,0,0,10"/>
        <TextBox x:Name="GameNameTextBox"
                 Margin="0,0,0,15"
                 FontSize="14"
                 Background="#AAE8F0F5"/>

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Save" Width="75" Margin="0,0,10,0" 
                    Click="SaveButton_Click" IsDefault="True"/>
            <Button Content="Cancel" Width="75" 
                    Click="CancelButton_Click" IsCancel="True"/>
        </StackPanel>
    </StackPanel>
</Window>